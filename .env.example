# ============================================
# The Wizard of AI - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values

# ============================================
# SUPABASE (RECOMMENDED - Database + Auth)
# ============================================
# Sign up at https://supabase.com and create a new project
# Find these values in: Settings > API

# Public URL and anon key (safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key (server-side only - NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# CONTACT FORM INTEGRATIONS (Optional)
# ============================================
# These are optional if Supabase is configured
# At least one integration should be configured for form submissions to work

# Option 1: Resend Email Integration (RECOMMENDED)
# Sign up at https://resend.com and get your API key
RESEND_API_KEY=re_xxxxxxxxxxxx

# Email to receive form notifications
NOTIFICATION_EMAIL=julian@thewizardofai.com

# Option 2: Generic Webhook (Make.com, Zapier, etc.)
# Use this if you want forms to trigger a webhook
NEXT_PUBLIC_FORM_WEBHOOK_URL=https://hook.make.com/your-webhook-id
# Or use server-side only (more secure):
FORM_WEBHOOK_URL=https://hook.make.com/your-webhook-id

# Option 3: n8n Webhook Integration
# Use this if you have an n8n instance for automation
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/contact-form

# ============================================
# AUTHENTICATION (Golden Ticket Portal)
# ============================================
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-key-change-this-in-production

# ============================================
# ENVIRONMENT
# ============================================
NODE_ENV=production

# ============================================
# ANALYTICS (Optional)
# ============================================
# Vercel Analytics is auto-enabled when deployed to Vercel
# No configuration needed

# ============================================
# CONFIGURATION NOTES
# ============================================
#
# SUPABASE SETUP (RECOMMENDED):
# 1. Go to https://supabase.com and create a new project
# 2. Go to Settings > API to find your keys
# 3. Copy the Project URL to NEXT_PUBLIC_SUPABASE_URL
# 4. Copy the anon public key to NEXT_PUBLIC_SUPABASE_ANON_KEY
# 5. Copy the service_role key to SUPABASE_SERVICE_ROLE_KEY
# 6. Run the SQL schema in scripts/supabase-schema.sql
#
# RESEND SETUP:
# 1. Sign up at https://resend.com
# 2. Verify your domain (thewizardofai.com)
# 3. Create an API key
# 4. Add the key to RESEND_API_KEY
#
# N8N SETUP:
# 1. Create a new workflow in n8n
# 2. Add a Webhook trigger node
# 3. Copy the production webhook URL
# 4. Add it to N8N_WEBHOOK_URL
#
# MAKE.COM / ZAPIER SETUP:
# 1. Create a new scenario/zap
# 2. Use a Webhook trigger
# 3. Copy the webhook URL
# 4. Add it to FORM_WEBHOOK_URL
#
# VERCEL DEPLOYMENT:
# Add these environment variables in your Vercel project settings:
# Settings > Environment Variables
